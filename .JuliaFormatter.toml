# JuliaFormatter configuration
# Keep it minimal and conservative; can be tuned later as needed.

indent = 4
margin = 92
always_for_in = true
remove_extra_newlines = true
short_to_long_function_def = false
always_use_return = false
